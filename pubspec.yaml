name: education_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router:
  animations:
  video_player:
  image_picker:
  json_annotation:
  path:
  path_provider:
  firebase_core:
  cloud_firestore:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  build_runner:
  json_serializable:

flutter:
  uses-material-design: true
  assets:
    - assets/
